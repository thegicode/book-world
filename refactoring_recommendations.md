# 리팩토링 제안 목록 (Book & LibrarySearchByBook)

`Book.ts`와 `LibrarySearchByBook.ts` 컴포넌트에서 발견된 문제점과 개선 방안입니다.

## 1. API 동시 요청 수 제어

**문제점:**
`LibrarySearchByBook.ts` 컴포넌트는 모든 지역의 도서관 정보를 얻기 위해 수십 개의 API 요청을 `Promise.all`을 사용하여 **동시에** 전송하고 있습니다. 이는 서버와 브라우저 양쪽에 과도한 부하를 주어 성능 저하 및 요청 실패의 원인이 될 수 있습니다.

**개선 방안:**
모든 요청을 한 번에 보내는 대신, **요청을 작은 그룹(batch)으로 나누어 순차적으로 처리**합니다. 예를 들어, 한 번에 5-10개의 요청만 동시에 처리하고, 해당 그룹이 완료되면 다음 그룹을 실행하는 방식으로 동시 연결 수를 제어합니다.

**기대 효과:**
-   **시스템 안정성 향상:** 서버와 클라이언트의 부하를 줄여 애플리케이션이 더 안정적으로 동작합니다.
-   **API 실패율 감소:** 과도한 동시 요청으로 인해 발생할 수 있는 네트워크 오류나 서버의 속도 제한(rate limiting)을 피할 수 있습니다.

## 2. API 응답 처리 로직 추상화

**문제점:**
`Book.ts`와 `LibrarySearchByBook.ts` 두 파일 모두에서 API 응답이 `{ status, data }` 형태로 래핑(wrapping)되어 있어, `response.data`를 추출하는 동일한 로직이 중복되고 있습니다. 이는 코드의 중복성을 높이고 유지보수를 어렵게 만듭니다.

**개선 방안:**
`CustomFetch.ts` 유틸리티에 `IApiResponse` 래퍼를 자동으로 처리하는 **공통 메서드 (예: `fetchData`)를 추가**합니다. 이 메서드는 API 호출 후 `status`가 'success'이면 `data`를 반환하고, 아닐 경우 에러를 발생시키도록 로직을 캡슐화합니다.

**기대 효과:**
-   **중복 감소 및 유지보수성 향상:** API 응답 처리 로직이 한 곳으로 중앙화되어 코드 중복이 사라지고, 향후 수정이 필요할 때 한 곳만 변경하면 됩니다.
-   **가독성 및 생산성 향상:** 각 컴포넌트에서는 `fetchData`를 호출하기만 하면 되므로 코드가 간결해지고 개발 생산성이 높아집니다.

## 3. 컴포넌트와 DOM의 결합도 낮추기

**문제점:**
컴포넌트의 렌더링 메서드 내에서 `querySelector`를 사용하여 DOM 요소를 직접 찾아 조작하는 방식은, 컴포넌트의 로직이 특정 HTML 구조에 강하게 의존하게 만듭니다. 이로 인해 마크업이 변경되면 JavaScript 코드도 함께 수정해야 하는 문제가 발생하며, 컴포넌트의 재사용성이 저하됩니다.

**개선 방안:**
-   **DOM 요소 캐싱:** `connectedCallback`과 같은 초기화 시점에 `querySelector`를 사용하여 필요한 DOM 요소들을 미리 찾아 클래스 속성(property)에 저장(캐싱)해 둡니다. 렌더링 시에는 이 저장된 속성을 재사용하여 반복적인 DOM 탐색을 피합니다.
-   **컴포넌트 스코프 유지:** `document.querySelector` 대신 컴포넌트 자신의 그림자 DOM(shadow DOM)이나 루트 내부를 탐색하는 `this.querySelector`를 사용하여 각 컴포넌트의 독립성을 보장합니다.

**기대 효과:**
-   **유지보수 용이성:** HTML 구조 변경이 JavaScript 코드에 미치는 영향을 최소화하여 유지보수가 쉬워집니다.
-   **성능 향상:** 불필요하고 반복적인 DOM 탐색을 줄여 렌더링 성능을 개선할 수 있습니다.

## 4. 사용자 피드백 개선

**문제점:**
`LibrarySearchByBook.ts`에서 특정 지역의 API 요청이 실패했을 때, 현재는 콘솔에만 경고가 기록될 뿐 사용자에게는 아무런 시각적 피드백이 제공되지 않습니다. 이로 인해 사용자는 데이터가 부분적으로 누락된 사실을 인지하지 못할 수 있습니다.

**개선 방안:**
API 요청이 실패하거나 데이터 로딩에 문제가 발생했을 때, **사용자가 명확히 인지할 수 있는 시각적 피드백을 UI에 제공**합니다. 예를 들어, "O개 지역의 도서관 정보를 불러오는 데 실패했습니다."와 같은 메시지를 화면에 표시합니다.

**기대 효과:**
-   **사용자 경험(UX) 개선:** 애플리케이션의 현재 상태를 사용자에게 투명하게 알려주어 신뢰도를 높이고 더 나은 사용 경험을 제공합니다.
-   **디버깅 용이성:** 개발자뿐만 아니라 사용자도 문제를 인지하고 피드백을 줄 수 있어 잠재적인 버그를 더 빨리 발견하는 데 도움이 됩니다.