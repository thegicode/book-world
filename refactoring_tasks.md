# 리팩토링 목록

다음은 이 프로젝트에 대해 제안되는 리팩토링 작업 목록입니다.

## 1. 백엔드: 컨트롤러 레이어 도입

**상태: 완료**

**문제점:**
현재 `server/src/routes/apiRoutes.ts` 파일의 라우트 핸들러가 API 데이터를 가져오는 로직을 직접 호출하고 있습니다. 이는 Express.js 애플리케이션의 일반적인 디자인 패턴인 **관심사 분리(Separation of Concerns)** 원칙을 위반합니다. 라우팅 로직과 비즈니스 로직이 강하게 결합되어 있어 코드의 유지보수, 테스트, 재사용이 어렵습니다.

**개선 방안:**
라우트 핸들러와 API 서비스 호출 사이에 **컨트롤러(Controller)** 레이어를 도입합니다.

*   **Routes:** HTTP 요청을 받고, 요청 파라미터나 바디를 파싱하여 컨트롤러의 적절한 메소드로 전달하는 역할만 담당합니다.
*   **Controllers:** 라우트로부터 받은 요청을 처리하는 비즈니스 로직을 포함합니다. 필요한 데이터를 얻기 위해 서비스(Service)나 모델(Model)을 호출하고, 그 결과를 클라이언트에게 반환할 형태로 가공하여 응답합니다.
*   **Services:** 외부 API 호출, 데이터베이스 연동 등 구체적인 데이터 처리 작업을 담당합니다.

**기대 효과:**
*   **모듈성 향상:** 각 레이어가 명확한 책임을 갖게 되어 코드 구조가 명확해지고, 기능 추가 및 변경이 용이해집니다.
*   **테스트 용이성:** 비즈니스 로직이 라우팅과 분리되어 단위 테스트 작성이 훨씬 쉬워집니다.
*   **재사용성 증가:** 특정 API에 종속되지 않는 비즈니스 로직을 다른 컨트롤러에서도 재사용할 수 있습니다.

## 2. 프론트엔드: 페이지별 로직 통합 및 재사용

**상태: 진행중**

**문제점:**
`app/src/scripts/pages/` 디렉토리 내의 여러 페이지 스크립트(예: `search/`, `library/`, `favorite/`)에 중복된 코드가 존재할 가능성이 높습니다. 예를 들어, API를 호출하고, 데이터를 파싱하며, DOM 요소를 만드는 등의 로직이 여러 파일에 걸쳐 반복될 수 있습니다.

**개선 방안:**
페이지 스크립트들을 분석하여 공통적으로 사용되는 기능들을 식별하고, 이를 별도의 **재사용 가능한 함수 또는 모듈**로 추출합니다.

*   **상태 관리 모델(`BookModel`) 리팩토링:**
    *   `BookModel`의 상태 관리 로직을 리팩토링하여 `localStorage` 접근을 최소화하고 성능을 개선했습니다.
    *   메모리 내 상태 캐시(`_state`)를 도입하여, 상태 변경 시마다 `localStorage`를 읽고 쓰는 비효율적인 구조를 개선했습니다.
    *   상태 변경 로직을 중앙화된 `_commit` 메서드로 통합하여 코드 중복을 제거하고 유지보수성을 향상시켰습니다.
*   **`FetchListComponent` 도입 및 `popular` 페이지 리팩토링:**
    *   `search` 페이지에서 사용되던 `FetchListComponent`를 활용하여 `popular` 페이지의 데이터 로딩 및 리스트 렌더링 로직을 리팩토링했습니다.
    *   `PopularList`와 `PopularItem` 컴포넌트를 새로 만들어, API 호출, 데이터 파싱, DOM 생성을 `FetchListComponent` 기반의 재사용 가능한 구조로 통합했습니다.
    *   이를 통해 `Popular.ts`의 코드가 크게 단순화되었고, 페이지의 책임이 명확하게 분리되었습니다.
*   **유틸리티 모듈(Utils):** DOM 조작, 날짜 포맷팅, 숫자 계산 등 범용적으로 사용될 수 있는 순수 함수들을 모아 `utils` 디렉토리에 관리합니다.
    *   - [ ] `fillElementsWithData` 헬퍼 함수를 개선하여, 템플릿에 해당 요소가 없을 경우 오류를 발생시키지 않고 안전하게 건너뛰도록 수정했습니다.
*   - [x] **API 서비스 모듈:** 서버 API를 호출하는 `fetch` 로직을 중앙에서 관리하는 모듈을 만듭니다. 이를 통해 API 요청/응답 처리, 에러 핸들링을 일관되게 처리할 수 있습니다. (완료: `CustomFetch.ts`를 `services` 디렉토리로 이동)
*   **상태 관리 모듈:** 여러 페이지에서 공유해야 하는 상태(예: 사용자 인증 정보, 지역 설정)가 있다면, 간단한 Pub/Sub 패턴이나 상태 관리 객체를 만들어 한 곳에서 관리합니다.

**기대 효과:**
*   **중복 감소:** 코드의 양이 줄어들고, 한 곳만 수정하면 모든 곳에 반영되므로 유지보수 비용이 감소합니다.
*   **가독성 및 일관성:** 공통 로직이 모듈화되어 전체적인 코드의 흐름을 파악하기 쉬워지고, 코드 스타일이 일관성을 갖게 됩니다.

## 3. 프론트엔드: 재사용 가능한 UI 컴포넌트 도입

**상태: 진행중**

**문제점:**
현재 애플리케이션은 별도의 프론트엔드 프레임워크 없이 순수 TypeScript로 작성되어, UI 요소들이 각 페이지 스크립트 내에서 직접 생성되고 조작되고 있습니다. 이는 비슷한 UI(예: 책 목록 아이템, 버튼, 네비게이션 바)를 여러 곳에서 만들어야 할 때 코드 중복을 야기하고, UI의 일관성을 해칠 수 있습니다.

**개선 방안:**
반복적으로 사용되는 UI 패턴을 식별하여 **재사용 가능한 컴포넌트(Component)** 형태로 모듈화합니다. 컴포넌트는 자체적인 HTML 구조, 스타일, 그리고 동작(이벤트 핸들링 등)을 캡슐화한 독립적인 단위입니다.

*   `app/public/html/templates/book-item.html` 와 같은 템플릿을 활용하여 동적으로 UI 컴포넌트를 생성하는 클래스나 함수를 만듭니다.
*   각 컴포넌트는 필요한 데이터를 받아 UI를 렌더링하고, 사용자 인터랙션에 따른 이벤트를 외부에 알리는 역할을 합니다.
*   예시: `BookItem`, `BookList`, `SearchBar`, `RegionSelector` 등

**진행 상황:**

*   **`BaseItemComponent` 도입:** 템플릿을 비동기적으로 로드하고 렌더링하는 로직을 공통화한 `BaseItemComponent`를 도입하여 컴포넌트 생성의 기반을 마련했습니다.
*   **`FavoriteItem` 컴포넌트 리팩토링:**
    *   `favorite.html`에 인라인으로 존재하던 템플릿을 `templates/favorite-item.html` 파일로 분리했습니다.
    *   `FavoriteItem`이 `BaseItemComponent`를 상속받아 자체적으로 템플릿을 로드하도록 수정하여 다른 컴포넌트와의 의존성을 제거하고 캡슐화를 강화했습니다.
*   **`BookItem` 컴포넌트 템플릿 수정:**
    *   `templates/book-item.html` 파일 내에 불필요하게 중첩된 `<template>` 태그를 제거하여 `BaseItemComponent`에서 템플릿을 정상적으로 파싱하고 렌더링할 수 있도록 수정했습니다.
*   **컴포넌트 로딩 오류 수정:** 템플릿 파일의 경로 문제 및 구조 문제를 해결하여, 즐겨찾기 페이지와 검색 결과 페이지에서 컴포넌트들이 정상적으로 렌더링되도록 수정했습니다.

**기대 효과:**
*   **UI 일관성:** 동일한 컴포넌트를 재사용하므로 애플리케이션 전체의 UI가 일관성을 갖게 됩니다.
*   **개발 효율성:** 이미 만들어진 컴포넌트를 조립하여 새로운 페이지를 더 빠르고 쉽게 구성할 수 있습니다.
*   **유지보수 용이성:** 특정 UI를 수정해야 할 때, 해당 컴포넌트 파일만 수정하면 되므로 변경의 영향 범위를 최소화할 수 있습니다.

## 4. 설정: `tsconfig.json` 파일 통합

**상태: 완료**

**문제점:**
프로젝트 내에 `tsconfig.json`, `tsconfig.test.json`, `server/src/tsconfig.json` 등 여러 개의 TypeScript 설정 파일이 존재합니다. 이 파일들 간에 중복된 설정이 많을 수 있으며, 이는 설정 변경 시 여러 파일을 수정해야 하는 번거로움을 유발합니다.

**개선 방안:**
TypeScript의 `extends` 옵션을 활용하여 설정 파일들을 계층적으로 구성하고 중복을 제거합니다.

1.  프로젝트의 가장 기본이 되는 설정을 `tsconfig.base.json`과 같은 공통 파일에 정의합니다.
2.  각각의 `tsconfig.json` 파일 (루트, 서버, 테스트)에서는 이 기본 설정을 `extends` 하고, 자신에게 필요한 특정 옵션만 추가하거나 덮어씁니다.

**기대 효과:**
*   **설정 중앙 관리:** 공통 설정이 한 곳에서 관리되므로 일관성 유지가 용이하고, 변경이 필요할 때 한 파일만 수정하면 됩니다.
*   **가독성 향상:** 각 설정 파일이 더 간결해져 어떤 부분이 특화된 설정인지 파악하기 쉬워집니다.

## 5. 오류 처리: 표준화 및 중앙화

**상태: 완료**

**문제점:**
클라이언트(프론트엔드)와 서버(백엔드) 양쪽에서 오류 처리 방식이 일관되지 않거나, 각 기능별로 흩어져 있을 수 있습니다. 예를 들어, 어떤 곳에서는 `try...catch`를 사용하고 다른 곳에서는 `Promise.catch()`를 사용하는 등 통일성이 없을 수 있습니다.

**개선 방안:**
애플리케이션의 오류 처리 전략을 표준화하고, 오류를 처리하는 로직을 중앙에서 관리합니다.

*   **백엔드:** Express.js의 오류 처리 미들웨어(Error-handling middleware)를 도입합니다. 모든 라우트 핸들러와 컨트롤러에서 발생하는 동기/비동기 오류를 마지막에 이 미들웨어가 받아서 처리하도록 합니다. 이를 통해 오류 로깅, 클라이언트에게 보낼 오류 메시지 포맷팅 등을 일관되게 수행할 수 있습니다. **(완료: `errorHandler` 미들웨어가 `server/src/index.ts`에 적용됨)**
*   **프론트엔드:** API 요청을 처리하는 서비스 모듈에서 `fetch` 응답을 분석하여, 서버에서 보낸 오류 상태 코드(예: 4xx, 5xx)에 따라 적절한 사용자 경험(예: 토스트 메시지, 에러 페이지)을 제공하는 공통 로직을 구현합니다. **(완료: `CustomFetch.ts`에서 오류 발생 시 토스트 메시지를 보여주도록 구현함)**

**기대 효과:**
*   **견고성(Robustness):** 예상치 못한 오류가 발생하더라도 애플리케이션이 비정상적으로 종료되지 않고, 사용자에게 적절한 피드백을 줄 수 있습니다.
*   **디버깅 용이성:** 오류가 발생하는 지점과 원인을 추적하기 쉬워지고, 중앙화된 로깅을 통해 오류 분석이 용이해집니다.
*   **일관된 사용자 경험:** 오류 상황에서 사용자에게 일관된 형태의 메시지를 보여줄 수 있습니다.

## 6. 백엔드: API 서비스 레이어 분할

**상태: 완료**

**문제점:**
`server/src/apis/index.ts` 파일의 `BookService`가 너무 커질 수 있는 단일 파일 형태로 여러 외부 API(Naver, Kyobo, Library 등) 호출 로직을 포함하고 있습니다. 이는 책임 분리 원칙에 위배되며, 코드의 유지보수성, 테스트 용이성, 가독성을 저해합니다.

**개선 방안:**
외부 API 호출 로직을 각 외부 API 제공업체별로 분리하여 더 작고 집중된 서비스 모듈로 나눕니다. (완료: `server/src/apis/index.ts` 파일을 리팩토링하여 각 API 파일을 재내보내도록 하여 모듈성 개선)

*   `NaverBookApiService.ts`: Naver 책 검색 API 관련 로직 캡슐화
*   `LibraryApiService.ts`: 도서관 정보나루 API 관련 로직 캡슐화 (월별 키워드, 도서관 검색 등)
*   `KyoboBookApiService.ts`: 교보문고 API 관련 로직 캡슐화

각 서비스는 해당 외부 API와의 통신만을 담당하며, `server/src/apis/index.ts`는 이들 개별 서비스를 한 곳에서 재내보내는(re-export) 역할만 수행합니다.

**기대 효과:**
*   **모듈성 향상:** 각 서비스의 책임이 명확해져 코드 이해 및 관리가 용이해집니다.
*   **유지보수성 증대:** 특정 외부 API의 변경사항이 발생하더라도 해당 서비스 파일만 수정하면 되므로 다른 부분에 미치는 영향이 최소화됩니다.
*   **테스트 용이성:** 각 서비스 모듈을 독립적으로 단위 테스트하기 쉬워집니다.
*   **확장성:** 새로운 외부 API 연동 시 기존 서비스에 영향을 주지 않고 쉽게 추가할 수 있습니다.

